// Custom template test - generated by OpenFeature CLI
package {{ .Params.Custom.GoPackage }}

// FlagKeys contains all feature flag keys
var FlagKeys = struct {
{{- range .Flagset.Flags }}
	{{ .Key | ToPascal }} string
{{- end }}
}{
{{- range .Flagset.Flags }}
	{{ .Key | ToPascal }}: {{ .Key | Quote }},
{{- end }}
}

