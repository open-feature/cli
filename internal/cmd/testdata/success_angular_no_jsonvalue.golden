/**
 * AUTOMATICALLY GENERATED BY OPENFEATURE CLI. DO NOT MODIFY MANUALLY.
 *
 * This file contains generated typesafe Angular services and directives
 * for feature flags defined in your OpenFeature flag manifest.
 *
 * Requires @openfeature/angular-sdk >= 1.1.0.
 *
 * @see https://openfeature.dev/docs/reference/other-technologies/cli
 */

import {
  ChangeDetectorRef,
  Directive,
  inject,
  Injectable,
  Input,
  OnChanges,
  TemplateRef,
  ViewContainerRef,
} from '@angular/core';
import {
  AngularFlagEvaluationOptions,
  EvaluationDetails,
  FeatureFlagDirective,
  FeatureFlagDirectiveContext,
  FeatureFlagService,
} from '@openfeature/angular-sdk';
import { Observable, map } from 'rxjs';

// ============================================================================
// FLAG KEYS
// ============================================================================

/**
 * Constant object containing all feature flag keys.
 * Use these constants to reference flag keys in a type-safe manner.
 */
export const FlagKeys = {
  /**
   * Flag key for Background color for the application.
   * - Type: `string`
   * - Default: `white`
   */
  BACKGROUND_COLOR: "backgroundColor",
  /**
   * Flag key for Enable dark mode.
   * - Type: `boolean`
   * - Default: `false`
   */
  DARK_MODE: "darkMode",
  /**
   * Flag key for Maximum number of items to display.
   * - Type: `number`
   * - Default: `10`
   */
  MAX_ITEMS: "maxItems",
} as const;

/**
 * Type representing all available flag keys.
 */
export type FlagKey = (typeof FlagKeys)[keyof typeof FlagKeys];

// ============================================================================
// GENERATED FEATURE FLAG SERVICE
// ============================================================================

/**
 * Generated typesafe feature flag service.
 * Provides strongly-typed methods for each feature flag defined in the manifest.
 *
 * @example
 * ```typescript
 * @Component({
 *   selector: 'app-my-component',
 *   template: `
 *     <div *ngIf="(myFlag$ | async)?.value">Feature enabled!</div>
 *   `
 * })
 * export class MyComponent {
 *   private flags = inject(GeneratedFeatureFlagService);
 *   myFlag$ = this.flags.getMyFlagDetails();
 * }
 * ```
 */
@Injectable({ providedIn: 'root' })
export class GeneratedFeatureFlagService {
  private readonly flagService = inject(FeatureFlagService);


  /**
   * Get evaluation details for the `backgroundColor` flag.
   *
   * Background color for the application
   *
   * **Details:**
   * - Flag key: `backgroundColor`
   * - Type: `string`
   * - Default value: `white`
   *
   * @param domain - Optional domain for flag evaluation (scopes the flag to a specific provider).
   * @param options - Optional configuration for the flag evaluation.
   * @returns An Observable that emits EvaluationDetails whenever the flag value changes.
   */
  getBackgroundColorDetails(
    domain?: string,
    options?: AngularFlagEvaluationOptions
  ): Observable<EvaluationDetails<string>> {
    return this.flagService.getStringDetails(
      "backgroundColor",
      "white",
      domain,
      {
        updateOnConfigurationChanged: options?.updateOnConfigurationChanged ?? true,
        updateOnContextChanged: options?.updateOnContextChanged ?? true,
      }
    );
  }

  /**
   * Get the value of the `backgroundColor` flag.
   *
   * Background color for the application
   *
   * @param domain - Optional domain for flag evaluation (scopes the flag to a specific provider).
   * @param options - Optional configuration for the flag evaluation.
   * @returns An Observable that emits the flag value whenever it changes.
   */
  getBackgroundColor(
    domain?: string,
    options?: AngularFlagEvaluationOptions
  ): Observable<string> {
    return this.getBackgroundColorDetails(domain, options).pipe(
      map((details) => details.value)
    );
  }

  /**
   * Get evaluation details for the `darkMode` flag.
   *
   * Enable dark mode
   *
   * **Details:**
   * - Flag key: `darkMode`
   * - Type: `boolean`
   * - Default value: `false`
   *
   * @param domain - Optional domain for flag evaluation (scopes the flag to a specific provider).
   * @param options - Optional configuration for the flag evaluation.
   * @returns An Observable that emits EvaluationDetails whenever the flag value changes.
   */
  getDarkModeDetails(
    domain?: string,
    options?: AngularFlagEvaluationOptions
  ): Observable<EvaluationDetails<boolean>> {
    return this.flagService.getBooleanDetails(
      "darkMode",
      false,
      domain,
      {
        updateOnConfigurationChanged: options?.updateOnConfigurationChanged ?? true,
        updateOnContextChanged: options?.updateOnContextChanged ?? true,
      }
    );
  }

  /**
   * Get the value of the `darkMode` flag.
   *
   * Enable dark mode
   *
   * @param domain - Optional domain for flag evaluation (scopes the flag to a specific provider).
   * @param options - Optional configuration for the flag evaluation.
   * @returns An Observable that emits the flag value whenever it changes.
   */
  getDarkMode(
    domain?: string,
    options?: AngularFlagEvaluationOptions
  ): Observable<boolean> {
    return this.getDarkModeDetails(domain, options).pipe(
      map((details) => details.value)
    );
  }

  /**
   * Get evaluation details for the `maxItems` flag.
   *
   * Maximum number of items to display
   *
   * **Details:**
   * - Flag key: `maxItems`
   * - Type: `number`
   * - Default value: `10`
   *
   * @param domain - Optional domain for flag evaluation (scopes the flag to a specific provider).
   * @param options - Optional configuration for the flag evaluation.
   * @returns An Observable that emits EvaluationDetails whenever the flag value changes.
   */
  getMaxItemsDetails(
    domain?: string,
    options?: AngularFlagEvaluationOptions
  ): Observable<EvaluationDetails<number>> {
    return this.flagService.getNumberDetails(
      "maxItems",
      10,
      domain,
      {
        updateOnConfigurationChanged: options?.updateOnConfigurationChanged ?? true,
        updateOnContextChanged: options?.updateOnContextChanged ?? true,
      }
    );
  }

  /**
   * Get the value of the `maxItems` flag.
   *
   * Maximum number of items to display
   *
   * @param domain - Optional domain for flag evaluation (scopes the flag to a specific provider).
   * @param options - Optional configuration for the flag evaluation.
   * @returns An Observable that emits the flag value whenever it changes.
   */
  getMaxItems(
    domain?: string,
    options?: AngularFlagEvaluationOptions
  ): Observable<number> {
    return this.getMaxItemsDetails(domain, options).pipe(
      map((details) => details.value)
    );
  }

}

// ============================================================================
// GENERATED STRUCTURAL DIRECTIVES
// ============================================================================



/**
 * Structural directive for the `backgroundColor` feature flag.
 *
 * Background color for the application
 *
 * This directive extends `FeatureFlagDirective` from @openfeature/angular-sdk
 * with a pre-configured flag key and default value.
 *
 * **Details:**
 * - Flag key: `backgroundColor`
 * - Type: `string`
 * - Default value: `white`
 *
 *
 * @example
 * Explicit `ng-template` (no `*`), bind inputs directly
 * ```html
 * <ng-template backgroundColorFeatureFlag
 *   [backgroundColorFeatureFlagDefault]="defaultValue"
 *   [backgroundColorFeatureFlagValue]="expectedValue"
 *   [backgroundColorFeatureFlagElse]="elseTemplate"
 *   [backgroundColorFeatureFlagInitializing]="initTemplate"
 *   [backgroundColorFeatureFlagReconciling]="reconcilingTemplate">
 *   <div>Content shown when flag is matched.</div>
 * </ng-template>
 * <ng-template #elseTemplate>
 *   Content shown when flag is not matched.
 * </ng-template>
 * ```
 *
 * @example
 * Microsyntax `*` form, start with `let`
 * ```html
 * <div *backgroundColorFeatureFlag="let v; value: expectedValue; else: elseTemplate; initializing: initTemplate; reconciling: reconcilingTemplate">
 *   Content shown when flag is matched.
 * </div>
 * ```
 *
 * @example
 * Simple `*` usage (no else/initializing/reconciling)
 * ```html
 * <div *backgroundColorFeatureFlag="let v">
 *   Content shown when flag is matched.
 * </div>
 * ```
 *
 * @remarks
 * Note: Angular's microsyntax parser requires the first segment to be a primary
 * expression or a `let` declaration. Because the flag key is preconfigured, start with
 * `let v` or `let details = evaluationDetails` (or `let _` if you do not need the value)
 * before any `else`/`initializing`/`reconciling`/`default` segments.
 * This is an Angular microsyntax parsing constraint, not a directive limitation.
 * `*backgroundColorFeatureFlag="else elseTemplate"` will not parse because `else` is a
 * secondary segment. If you do not need the value, use `let _`, or use the
 * explicit `ng-template` form above.
 */
@Directive({
  selector: '[backgroundColorFeatureFlag]',
  standalone: true,
})
export class BackgroundColorFeatureFlagDirective extends FeatureFlagDirective<string> implements OnChanges {
  override _changeDetectorRef = inject(ChangeDetectorRef);
  override _viewContainerRef = inject(ViewContainerRef);
  override _thenTemplateRef = inject<TemplateRef<FeatureFlagDirectiveContext<string>>>(TemplateRef);

  /**
   * The expected value of this string feature flag, for which the `then` template should be rendered.
   */
  @Input({ required: false }) backgroundColorFeatureFlagValue?: string;

  constructor() {
    super();

    this._featureFlagKey = "backgroundColor";
    this._featureFlagDefault = "white";

  }

  override ngOnChanges() {
    super.ngOnChanges();

    this._featureFlagValue = this.backgroundColorFeatureFlagValue;
  }

  /**
   * The domain of the string feature flag.
   */
  @Input({ required: false })
  set backgroundColorFeatureFlagDomain(domain: string | undefined) {
    super.featureFlagDomain = domain;
  }

  /**
   * Update the component if the provider emits a ConfigurationChanged event.
   * Set to false to prevent components from re-rendering when flag value changes
   * are received by the associated provider.
   * Defaults to true.
   */
  @Input({ required: false })
  set backgroundColorFeatureFlagUpdateOnConfigurationChanged(enabled: boolean | undefined) {
    this._updateOnConfigurationChanged = enabled ?? true;
  }

  /**
   * Update the component when the OpenFeature context changes.
   * Set to false to prevent components from re-rendering when attributes which
   * may be factors in flag evaluation change.
   * Defaults to true.
   */
  @Input({ required: false })
  set backgroundColorFeatureFlagUpdateOnContextChanged(enabled: boolean | undefined) {
    this._updateOnContextChanged = enabled ?? true;
  }

  /**
   * Template to be displayed when the feature flag does not match value.
   */
  @Input()
  set backgroundColorFeatureFlagElse(tpl: TemplateRef<FeatureFlagDirectiveContext<string>>) {
    this._elseTemplateRef = tpl;
  }

  /**
   * Template to be displayed when the provider is not ready.
   */
  @Input()
  set backgroundColorFeatureFlagInitializing(tpl: TemplateRef<FeatureFlagDirectiveContext<string>>) {
    this._initializingTemplateRef = tpl;
  }

  /**
   * Template to be displayed when the provider is reconciling.
   */
  @Input()
  set backgroundColorFeatureFlagReconciling(tpl: TemplateRef<FeatureFlagDirectiveContext<string>>) {
    this._reconcilingTemplateRef = tpl;
  }
}



/**
 * Structural directive for the `darkMode` feature flag.
 *
 * Enable dark mode
 *
 * This directive extends `FeatureFlagDirective` from @openfeature/angular-sdk
 * with a pre-configured flag key and default value.
 *
 * **Details:**
 * - Flag key: `darkMode`
 * - Type: `boolean`
 * - Default value: `false`
 *
 *
 * @example
 * Explicit `ng-template` (no `*`), bind inputs directly
 * ```html
 * <ng-template darkModeFeatureFlag
 *   [darkModeFeatureFlagDefault]="defaultValue"
 *   [darkModeFeatureFlagElse]="elseTemplate"
 *   [darkModeFeatureFlagInitializing]="initTemplate"
 *   [darkModeFeatureFlagReconciling]="reconcilingTemplate">
 *   <div>Content shown when flag is enabled.</div>
 * </ng-template>
 * <ng-template #elseTemplate>
 *   Content shown when flag is disabled.
 * </ng-template>
 * ```
 *
 * @example
 * Microsyntax `*` form, start with `let`
 * ```html
 * <div *darkModeFeatureFlag="let v; else: elseTemplate; initializing: initTemplate; reconciling: reconcilingTemplate">
 *   Content shown when flag is enabled.
 * </div>
 * ```
 *
 * @example
 * Simple `*` usage (no else/initializing/reconciling)
 * ```html
 * <div *darkModeFeatureFlag="let v">
 *   Content shown when flag is enabled.
 * </div>
 * ```
 *
 * @remarks
 * Note: Angular's microsyntax parser requires the first segment to be a primary
 * expression or a `let` declaration. Because the flag key is preconfigured, start with
 * `let v` or `let details = evaluationDetails` (or `let _` if you do not need the value)
 * before any `else`/`initializing`/`reconciling`/`default` segments.
 * This is an Angular microsyntax parsing constraint, not a directive limitation.
 * `*darkModeFeatureFlag="else elseTemplate"` will not parse because `else` is a
 * secondary segment. If you do not need the value, use `let _`, or use the
 * explicit `ng-template` form above.
 */
@Directive({
  selector: '[darkModeFeatureFlag]',
  standalone: true,
})
export class DarkModeFeatureFlagDirective extends FeatureFlagDirective<boolean> implements OnChanges {
  override _changeDetectorRef = inject(ChangeDetectorRef);
  override _viewContainerRef = inject(ViewContainerRef);
  override _thenTemplateRef = inject<TemplateRef<FeatureFlagDirectiveContext<boolean>>>(TemplateRef);

  constructor() {
    super();

    this._featureFlagKey = "darkMode";
    this._featureFlagDefault = false;

    this._featureFlagValue = true;

  }

  /**
   * The domain of the boolean feature flag.
   */
  @Input({ required: false })
  set darkModeFeatureFlagDomain(domain: string | undefined) {
    super.featureFlagDomain = domain;
  }

  /**
   * Update the component if the provider emits a ConfigurationChanged event.
   * Set to false to prevent components from re-rendering when flag value changes
   * are received by the associated provider.
   * Defaults to true.
   */
  @Input({ required: false })
  set darkModeFeatureFlagUpdateOnConfigurationChanged(enabled: boolean | undefined) {
    this._updateOnConfigurationChanged = enabled ?? true;
  }

  /**
   * Update the component when the OpenFeature context changes.
   * Set to false to prevent components from re-rendering when attributes which
   * may be factors in flag evaluation change.
   * Defaults to true.
   */
  @Input({ required: false })
  set darkModeFeatureFlagUpdateOnContextChanged(enabled: boolean | undefined) {
    this._updateOnContextChanged = enabled ?? true;
  }

  /**
   * Template to be displayed when the feature flag is false.
   */
  @Input()
  set darkModeFeatureFlagElse(tpl: TemplateRef<FeatureFlagDirectiveContext<boolean>>) {
    this._elseTemplateRef = tpl;
  }

  /**
   * Template to be displayed when the provider is not ready.
   */
  @Input()
  set darkModeFeatureFlagInitializing(tpl: TemplateRef<FeatureFlagDirectiveContext<boolean>>) {
    this._initializingTemplateRef = tpl;
  }

  /**
   * Template to be displayed when the provider is reconciling.
   */
  @Input()
  set darkModeFeatureFlagReconciling(tpl: TemplateRef<FeatureFlagDirectiveContext<boolean>>) {
    this._reconcilingTemplateRef = tpl;
  }
}



/**
 * Structural directive for the `maxItems` feature flag.
 *
 * Maximum number of items to display
 *
 * This directive extends `FeatureFlagDirective` from @openfeature/angular-sdk
 * with a pre-configured flag key and default value.
 *
 * **Details:**
 * - Flag key: `maxItems`
 * - Type: `number`
 * - Default value: `10`
 *
 *
 * @example
 * Explicit `ng-template` (no `*`), bind inputs directly
 * ```html
 * <ng-template maxItemsFeatureFlag
 *   [maxItemsFeatureFlagDefault]="defaultValue"
 *   [maxItemsFeatureFlagValue]="expectedValue"
 *   [maxItemsFeatureFlagElse]="elseTemplate"
 *   [maxItemsFeatureFlagInitializing]="initTemplate"
 *   [maxItemsFeatureFlagReconciling]="reconcilingTemplate">
 *   <div>Content shown when flag is matched.</div>
 * </ng-template>
 * <ng-template #elseTemplate>
 *   Content shown when flag is not matched.
 * </ng-template>
 * ```
 *
 * @example
 * Microsyntax `*` form, start with `let`
 * ```html
 * <div *maxItemsFeatureFlag="let v; value: expectedValue; else: elseTemplate; initializing: initTemplate; reconciling: reconcilingTemplate">
 *   Content shown when flag is matched.
 * </div>
 * ```
 *
 * @example
 * Simple `*` usage (no else/initializing/reconciling)
 * ```html
 * <div *maxItemsFeatureFlag="let v">
 *   Content shown when flag is matched.
 * </div>
 * ```
 *
 * @remarks
 * Note: Angular's microsyntax parser requires the first segment to be a primary
 * expression or a `let` declaration. Because the flag key is preconfigured, start with
 * `let v` or `let details = evaluationDetails` (or `let _` if you do not need the value)
 * before any `else`/`initializing`/`reconciling`/`default` segments.
 * This is an Angular microsyntax parsing constraint, not a directive limitation.
 * `*maxItemsFeatureFlag="else elseTemplate"` will not parse because `else` is a
 * secondary segment. If you do not need the value, use `let _`, or use the
 * explicit `ng-template` form above.
 */
@Directive({
  selector: '[maxItemsFeatureFlag]',
  standalone: true,
})
export class MaxItemsFeatureFlagDirective extends FeatureFlagDirective<number> implements OnChanges {
  override _changeDetectorRef = inject(ChangeDetectorRef);
  override _viewContainerRef = inject(ViewContainerRef);
  override _thenTemplateRef = inject<TemplateRef<FeatureFlagDirectiveContext<number>>>(TemplateRef);

  /**
   * The expected value of this number feature flag, for which the `then` template should be rendered.
   */
  @Input({ required: false }) maxItemsFeatureFlagValue?: number;

  constructor() {
    super();

    this._featureFlagKey = "maxItems";
    this._featureFlagDefault = 10;

  }

  override ngOnChanges() {
    super.ngOnChanges();

    this._featureFlagValue = this.maxItemsFeatureFlagValue;
  }

  /**
   * The domain of the number feature flag.
   */
  @Input({ required: false })
  set maxItemsFeatureFlagDomain(domain: string | undefined) {
    super.featureFlagDomain = domain;
  }

  /**
   * Update the component if the provider emits a ConfigurationChanged event.
   * Set to false to prevent components from re-rendering when flag value changes
   * are received by the associated provider.
   * Defaults to true.
   */
  @Input({ required: false })
  set maxItemsFeatureFlagUpdateOnConfigurationChanged(enabled: boolean | undefined) {
    this._updateOnConfigurationChanged = enabled ?? true;
  }

  /**
   * Update the component when the OpenFeature context changes.
   * Set to false to prevent components from re-rendering when attributes which
   * may be factors in flag evaluation change.
   * Defaults to true.
   */
  @Input({ required: false })
  set maxItemsFeatureFlagUpdateOnContextChanged(enabled: boolean | undefined) {
    this._updateOnContextChanged = enabled ?? true;
  }

  /**
   * Template to be displayed when the feature flag does not match value.
   */
  @Input()
  set maxItemsFeatureFlagElse(tpl: TemplateRef<FeatureFlagDirectiveContext<number>>) {
    this._elseTemplateRef = tpl;
  }

  /**
   * Template to be displayed when the provider is not ready.
   */
  @Input()
  set maxItemsFeatureFlagInitializing(tpl: TemplateRef<FeatureFlagDirectiveContext<number>>) {
    this._initializingTemplateRef = tpl;
  }

  /**
   * Template to be displayed when the provider is reconciling.
   */
  @Input()
  set maxItemsFeatureFlagReconciling(tpl: TemplateRef<FeatureFlagDirectiveContext<number>>) {
    this._reconcilingTemplateRef = tpl;
  }
}



// ============================================================================
// EXPORTS
// ============================================================================

/**
 * Array of all generated feature flag directives.
 * Import this in your module or standalone component to use the directives.
 *
 * @example
 * ```typescript
 * @Component({
 *   standalone: true,
 *   imports: [GeneratedFeatureFlagDirectives],
 *   template: `<div *myFeatureFlag>...</div>`
 * })
 * export class MyComponent {}
 * ```
 */
export const GeneratedFeatureFlagDirectives = [
  BackgroundColorFeatureFlagDirective,
  DarkModeFeatureFlagDirective,
  MaxItemsFeatureFlagDirective,
] as const;
